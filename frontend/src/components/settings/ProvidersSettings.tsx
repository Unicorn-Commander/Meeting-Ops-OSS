import ProviderSettingsPanel from '../ProviderSettingsPanel';
import type { SectionProps } from './SettingsContext';

export default function ProvidersSettings(_props: SectionProps) {
  return <ProviderSettingsPanel />;
}
